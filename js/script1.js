$(document).ready(function () {
     
    //image height
    var winHeight = $(window).height();
    var winHeightImg = $(window).height() - 140;
    $('container').css('height', winHeightImg);
    $('container').css('height', winHeight);
   

})

